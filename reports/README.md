Final exported deliverables should be stored in this folder.

Expected final files:

- `project_report.pdf`
- `presentation.pdf`

Use the markdown templates in this folder as drafting guides before exporting the final PDFs.

Recommended workflow:

1. Draft content in `project_report_template.md`.
2. Draft slide narrative in `presentation_outline.md`.
3. Cross-check metrics with `data/processed/etl_summary.json`.
4. Export the final files as `project_report.pdf` and `presentation.pdf`.
